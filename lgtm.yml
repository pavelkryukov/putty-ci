extraction:
  cpp:
     configure:
        command:
        - ./mkfiles.pl
        - autoreconf -fi
        - ./configure
     index:
        build_command:
        - pushd ./doc && make man && popd
        - make
