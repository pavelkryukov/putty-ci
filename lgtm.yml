extraction:
  cpp:
     configure:
        command:
        - mkdir build
        - cd build
        - cmake -G Ninja ..
     index:
        build_command:
        - pushd ./doc && make man && popd
        - cd build
        - ninja all
